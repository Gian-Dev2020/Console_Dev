using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sensors : MonoBehaviour
{
    [SerializeField] AgentController.AgentType agentType;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // depending on agent type will chaneg which sensors are active

    // sound sensor

    //sight sensor

    
}
